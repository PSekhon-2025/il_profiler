# secrets
.env
.env.*
!.env.example

# environments
.venv/
venv/

# generated data (vector store + results)
data/

# corpus PDFs published for the audit tab's excerpt links, copied on first
# reference from IL_PROFILER_DATASET_ROOT — derived, and not ours to ship
static/

# python
__pycache__/
*.pyc

# os
.DS_Store
